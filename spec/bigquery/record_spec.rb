require "spec_helper"

describe Bigquery::Record do
  it "has a version number" do
    expect(Bigquery::Record::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
